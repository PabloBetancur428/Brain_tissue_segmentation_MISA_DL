Run files in the following order:

1) Registration_img_landmarks
	Out: transformed points in txt file. They are in transformix format, so it must be 	changed by the next code.
2) Points_extraction
	This code extract the information of interest from the output txt file
3) Compute_TREç
	TRE between transformed landmarks and fixed landmarks